name: Contribute

on:
  issues:
    types: [opened]

jobs:
  add:
    runs-on: ubuntu-latest
    if: ${{ contains(github.event.issue.body, '## Map Contribution') }}
    steps:
      - run: echo "Matched"
